package travel.snapshot.qa.category;

/**
 * Put this class into the Category annotation in case such test case class has something to do with Tomcat being set up
 * on the host machine. There is need for clean Tomcat container with admin:admin credentials locally.
 */
public interface TomcatTest {
}
