<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>

    <!-- Needed to ensure that the mapper is loaded before other mappers -->
    <mappers>
        <mapper resource="travel/snapshot/dp/qa/nonpms/dwh/test/dal/DwhDaoMapper.xml"/>
    </mappers>

</configuration>
