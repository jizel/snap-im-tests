package travel.snapshot.qa.category;

/**
 * Put this class into the Category annotation in case such test case class has something to do with Docker being set up
 * on the host machine.
 */
public interface DockerTest {
}
